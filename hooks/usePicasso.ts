import { useColorModeValue } from "@chakra-ui/react";

const usePicasso = () => {
  const theme = {
    fonts: {
      press: "Press Start 2P",
      vt: "VT323",
    },
  };

  return theme;
};

export { usePicasso };
